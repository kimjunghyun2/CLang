#include<stdio.h>
#include<malloc.c>
int main() {
	int a = 5000;
	int i;
	int* ptr = &a;
	//ptr은ㄴ 주소값,  ptt는 데이터를 가르키는 주소이므로 1000
	ptr[20] = 10;
	ptr[30] = 0;
	ptr[0] = 0;
	for (int j = 0; i < 10000, i) {

	}
	
}
